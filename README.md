# Wazuh-Project


# Wazuh SIEM Lab

## Overview

This project documents the deployment and configuration of a Wazuh-based security monitoring lab. The environment will be used to collect endpoint telemetry, create and test detections, investigate security events, and simulate activity that may be encountered by a SOC analyst.

## Wazuh Server

The Wazuh server was deployed on an Ubuntu Server 24.04 LTS virtual machine.

### Initial Configuration

- Installed Ubuntu Server 24.04 LTS
- Enabled OpenSSH during installation for remote administration
- Installed Wazuh

The Ubuntu VM will act as the central Wazuh server for the lab, receiving and analysing telemetry from endpoints added later in the project.


## Accessing the Wazuh Dashboard

After installation, the Wazuh web interface was accessed through a browser using the IP address of the Wazuh server.

The dashboard provides the main interface for monitoring the environment, viewing security alerts, investigating events, managing agents, and configuring Wazuh.

<img width="2559" height="1116" alt="image" src="https://github.com/user-attachments/assets/599fa94b-eaa1-43a7-aa89-fa3951b8c37a" />


## Enabling Wazuh Archives

By default, Wazuh primarily focuses on events that match rules and generate alerts. For this lab, **archives were enabled so that Wazuh retains all received telemetry**, including events that do not trigger an alert.

This is useful in a SOC lab because an event may still be valuable during an investigation even if it did not initially match a detection rule. Retaining the raw telemetry also allows historical events to be searched, suspicious behaviour to be investigated, and new detection rules to be developed from previously collected data.

### Enable Archives on the Wazuh Manager

The Wazuh manager configuration file was modified:

```text
/var/ossec/etc/ossec.conf
```

The following settings were changed to `yes`:

```xml
<logall>yes</logall>
<logall_json>yes</logall_json>
```

- `logall` enables Wazuh to archive all received events.
- `logall_json` stores the archived events in JSON format, making them suitable for indexing and searching.

After making changes to `ossec.conf`, the Wazuh manager must be restarted for the configuration to take effect:

```bash
systemctl restart wazuh-manager.service
```

### Configure Filebeat

Although Wazuh is now archiving the events, Filebeat also needs to be configured to send the archived data to the Wazuh indexer so that it can be searched through the dashboard.

The Filebeat configuration file is located at:

```text
/etc/filebeat/filebeat.yml
```

I opened the configuration:

```bash
nano /etc/filebeat/filebeat.yml
```

Archive indexing was enabled by changing:

```yaml
archives:
  enabled: true
```

Filebeat was then restarted to apply the change:

```bash
systemctl restart filebeat
```

### Create the Archives Index Pattern

In the Wazuh web interface, I navigated to:

**Dashboard Management → Index Patterns**

There was no existing index pattern for the Wazuh archives, so I created:

```text
wazuh-archives-*
```
<img width="1240" height="532" alt="image" src="https://github.com/user-attachments/assets/5f17d3e6-b3dd-4d6d-a24d-91877200a9fb" />

<img width="1039" height="521" alt="image" src="https://github.com/user-attachments/assets/a75c6043-d11a-4c9e-80be-cf60f2eb7b0f" />

This makes the archived telemetry available for searching and analysis through the Wazuh dashboard.

### Result

Wazuh archives are now enabled and Filebeat is configured to index the archived events. The `wazuh-archives-*` index pattern was successfully created and the raw telemetry can now be accessed through the web interface.

<img width="2539" height="1262" alt="image" src="https://github.com/user-attachments/assets/3b8df5e5-ff5c-42d6-ab91-96dd0fe5f17f" />



## Endpoint Deployment and Sysmon Integration

To begin collecting endpoint telemetry, I created two additional virtual machines:

- **Windows 10**
- **Ubuntu Linux**

Both machines were added to Wazuh using the **Deploy New Agent** option within the Wazuh dashboard. I selected the appropriate operating system for each endpoint and used the installation commands generated by Wazuh to install and register the agents.

Once deployed, both endpoints appeared within Wazuh and were able to communicate with the Wazuh server.

<img width="2533" height="585" alt="image" src="https://github.com/user-attachments/assets/0a7b94dd-9939-4f44-9f27-f1c0bfc74979" />

## Windows Sysmon Configuration

To improve the telemetry available from the Windows endpoint, I installed **Sysmon (System Monitor)**.

Sysmon is part of Microsoft's Sysinternals suite and runs as a Windows system service. It records detailed information about activity occurring on an endpoint that is particularly useful for security monitoring and incident investigation.

This can include events such as:

- Process creation
- Network connections
- File creation
- Registry activity
- Process access
- Driver and image loading
- DNS queries

This provides much greater visibility into endpoint behaviour than relying only on standard Windows event logs.

### Olaf Hartong Sysmon Configuration

Rather than using Sysmon with a basic configuration, I used the community-maintained **Olaf Hartong Sysmon configuration**.

The configuration defines which activity Sysmon should record and which events should be excluded. It is designed with security monitoring and threat detection in mind, providing useful telemetry while filtering some of the unnecessary noise that would otherwise be generated.

This makes it particularly suitable for a SOC/SIEM environment where Sysmon telemetry will later be used for detection and investigation.

Sysmon was installed with the configuration using:

```powershell
.\Sysmon.exe -i .\sysmonconfig.xml
```

## Forwarding Windows Sysmon Events to Wazuh

Installing Sysmon causes Windows to begin generating the additional telemetry, but the Wazuh agent also needs to be instructed to collect the Sysmon event channel.

The Wazuh agent configuration is stored at:

```text
C:\Program Files (x86)\ossec-agent\ossec.conf
```

Because the file requires administrative permissions to modify, I opened **Notepad as Administrator**, selected **All Files**, and opened `ossec.conf`.

The following configuration was added:

```xml
<localfile>
  <location>Microsoft-Windows-Sysmon/Operational</location>
  <log_format>eventchannel</log_format>
</localfile>
```

This instructs the Wazuh agent to monitor the Windows Sysmon Operational event channel and forward those events to the Wazuh server.

The Wazuh agent was then restarted to apply the configuration.

<img width="673" height="462" alt="image" src="https://github.com/user-attachments/assets/1709fdf3-3484-485f-b61e-de9cce9a8104" />

## Verifying Windows Sysmon Telemetry

After restarting the agent, I returned to Wazuh and searched the telemetry for Sysmon events.

Sysmon results were now visible.

<img width="2556" height="1258" alt="image" src="https://github.com/user-attachments/assets/f103b5a8-9812-419d-9fa7-7bba00d53f01" />


Opening one of the events showed the following field:

```text
data.win.system.channel:
Microsoft-Windows-Sysmon/Operational
```

<img width="1082" height="43" alt="image" src="https://github.com/user-attachments/assets/11c87444-be9f-4d34-bb54-cf067b404213" />


This confirmed that the events originated from the Sysmon Operational event channel and that the pipeline was working correctly:

```text
Sysmon
   ↓
Windows Event Log
   ↓
Wazuh Agent
   ↓
Wazuh Server
   ↓
Wazuh Indexer / Dashboard
```

## Linux Sysmon Configuration

I then configured Sysmon on the Ubuntu endpoint to provide similar additional telemetry from the Linux system.

Sysmon for Linux was installed using Microsoft's official installation instructions:

```text
https://github.com/microsoft/SysmonForLinux/blob/main/INSTALL.md
```

### Installing the Sysmon Configuration

For the Linux endpoint, I used Microsoft's `collect-all.xml` configuration from the **MSTIC-Sysmon** repository.

The configuration was downloaded using:

```bash
wget https://raw.githubusercontent.com/microsoft/MSTIC-Sysmon/refs/heads/main/linux/configs/collect-all.xml
```

The configuration is intentionally broad and collects a large amount of available Sysmon telemetry. This is useful within the lab because I want high visibility into endpoint activity while learning what information is available and developing detections.

Sysmon was installed using the downloaded configuration:

```bash
sudo sysmon -i collect-all.xml
```

## Linux Sysmon Logging

On this Ubuntu endpoint, the Sysmon events are written into the Linux logging infrastructure and can be observed within:

```text
/var/log/syslog
```

The Wazuh agent can then collect the relevant system logs and forward them to the Wazuh server.

The Linux telemetry pipeline therefore looks approximately like:

```text
Sysmon for Linux
      ↓
/var/log/syslog
      ↓
Wazuh Agent
      ↓
Wazuh Server
      ↓
Wazuh Indexer / Dashboard
```

## Verifying Linux Sysmon Telemetry

To verify that Sysmon telemetry was reaching Wazuh, I switched the dashboard filter to the **Linux endpoint** and searched for:

```text
sysmon
```

Sysmon-related events were visible in the results.

<img width="2554" height="857" alt="image" src="https://github.com/user-attachments/assets/4c29cb53-807b-4b5c-adb9-7007bb0d5793" />

This confirmed that Sysmon was successfully generating telemetry on the Ubuntu endpoint and that the events were being collected by Wazuh.

## Result

At this stage, the lab consists of a central Wazuh server monitoring both Windows and Linux endpoints:

```text
                    Wazuh Server
                         │
              ┌──────────┴──────────┐
              │                     │
       Windows 10 Agent       Ubuntu Agent
              │                     │
     Sysmon for Windows      Sysmon for Linux
              │                     │
   Sysmon Operational Log       /var/log/syslog
```

Both endpoints are successfully communicating with Wazuh and providing Sysmon telemetry. This gives the lab considerably greater endpoint visibility and provides a foundation for developing detections and investigating simulated attacks later in the project.



